/**
 * Make sure vercel is running locally if using local url
 * from within vercel-vancouver-pools-proxy, run:
 * vercel dev --listen 3001
 */
// const VERCEL_URL = 'http://localhost:3001/api/app'
const VERCEL_URL = 'https://vercel-vancouver-pools-proxy.vercel.app/api/app'

export default VERCEL_URL
